"use client";

// This component has been deprecated as the application now uses only light theme
// Keeping a placeholder component for backward compatibility

export function ThemeSwitcher() {
  return null; // Return null since we no longer need this component
}
